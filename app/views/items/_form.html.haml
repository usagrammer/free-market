.block
  .block__menu
    商品の情報を入力
= form_with local: true, html: {id: "item_form"} do |f|
  .block.horizontal-padding-5
    .input-field
      .input-field-main
        = f.label :detail, "出品画像"
        %span.required-true
          必須
        %br
        最大5枚までアップロードできます。
        #selected-item-images
          -# 商品編集の際はプレビューを表示
          - if false
            .item-image{data: {index: 0}}
              = image_tag "", class: "item-image__image"
              .item-image__buttons
                .item-image__buttons--edit
                  編集
                .item-image__buttons--delete
                  削除
          #select-image-button
            #select-image-button__text
              クリックしてファイルをアップロード
    #image-file-fields
      = file_field_tag :hoge, class: "new-item-image", id: "file_field_index_0", accept: "image/*", data: {index: 0}
    .error-field{data:{column_name: "item_images"}}
  .block.horizontal-padding-5
    .input-field
      .input-field-main
        = f.label :hoge, "商品名"
        %span.required-true
          必須
        %br
        = f.text_field :hoge, required: true, placeholder: "PC・携帯どちらも可"
        .error-field{data:{column_name: "name"}}
    .input-field
      .input-field-main
        = f.label :hoge, "商品の説明"
        %span.required-true
          必須
        %br
        = f.text_area :hoge, required: true, placeholder: "6文字以上", rows: 5
        .error-field{data:{column_name: "detail"}}
  .block.horizontal-padding-5
    .input-field
      .input-field-title
        .input-field-title__text
          商品の詳細
      .input-field-main
        = f.label :hoge, "カテゴリ"
        %span.required-true
          必須
        = f.collection_select :hoge, [], :hoge , :hoge, {include_blank: "---"}, required: true, class: "select-category", id: "parent-category"
        .error-field{data:{column_name: "category"}}

        = f.label :hoge, for: "brand-search__form", class: "select-category" do
          ブランド
        %span.required-false.select-category
          任意
        #brand-search.select-category
          = f.text_field  :hoge, name: "brand_name", id: "brand-search__form"
          #brand-search__result
        .error-field.select-category{data:{column_name: "brand"}}

        = f.label :hoge, for: "size-form", class: "select-category" do
          サイズ
        %span.required-true.select-category
          必須
        .select-category
          = f.collection_select :hoge, [], :hoge, :hoge, {include_blank: "---"}, {id: "size-form", required: true}
          .error-field.select-category{data:{column_name: "item_size.size"}}

        = f.label :hoge, "商品の状態"
        %span.required-true
          必須
        %br
        = f.select :hoge, []
  .block.horizontal-padding-5
    .input-field
      .input-field-title
        .input-field-title__text
          配送について
      .input-field-main
        = f.label :hoge, "配送料の負担"
        %span.required-true
          必須
        %br
        = f.select :hoge, [], {include_blank: "---"}
        .error-field{data:{column_name: "delivery_fee_payer"}}
        %br
        .input-field-main__delivery-method
          = f.label :hoge, "配送の方法"
          %span.required-true
            必須
          %br
          = f.select :hoge, []
          %br
          .error-field{data:{column_name: "delivery_method"}}
        = f.label :hoge, "発送元の地域"
        %span.required-true
          必須
        %br
        = f.collection_select :hoge, [], :hoge , :hoge, {include_blank: "---"}, required: true, placeholder: "発送元の地域"
        .error-field{data:{column_name: "delivery_agency"}}
        %br
        = f.label :hoge, "発送までの日数"
        %span.required-true
          必須
        %br
        = f.select :hoge, []
        .error-field{data:{column_name: "delivery_days"}}
  .block.horizontal-padding-5
    .input-field
      .input-field-title
        .input-field-title__text
          価格（300〜9,999,999）
      .input-field-main
        .input-field-main-price
          .input-field-main-price__left
            = f.label :hoge, "価格"
            %span.required-true
              必須
          .input-field-main-price__right
            = f.text_field :hoge, required: true, placeholder: "価格"
            .error-field{data:{column_name: "price"}}
        %hr
        .input-field-main-price
          .input-field-main-price__left
            販売手数料（10%）
          .input-field-main-price__right
            #fee
        %hr
        .input-field-main-price
          .input-field-main-price__left
            販売利益
          .input-field-main-price__right
            #profit
  .block.horizontal-padding-5
    .information
      .information__text
        禁止されている出品、行為を必ずご確認ください。
        %br
        またブランド品でシリアルナンバー等がある場合はご記載ください。偽ブランドの販売は犯罪であり処罰される可能性があります。
        %br
        また、出品をもちまして加盟店規約に同意したことになります。
  .block.horizontal-padding-5
    = f.submit "出品する", class:"button back-red font-white"
    = link_to "", method: :delete do
      .button.back-gray
        商品を削除する
    .horizontal-padding-25
      = link_to "" do
        .button.back-gray
          戻る
%script{ src: "https://cdnjs.cloudflare.com/ajax/libs/fuse.js/3.3.1/fuse.min.js" }
